# 项目执行规范（AGENTS 铁律 · 最高优先级）

> 本文件是本项目的最高优先级执行规范。**无论新开多少会话，都必须严格执行。**
> 简版已同步进 `.workbuddy/memory/MEMORY.md`，每次会话自动注入；本文件为可人工编辑的权威版本。
> 适用范围：整个 `SaaS/` 项目（贷前审核、报告模板、贷中监控及之后所有模块）。

---

## 1. 只写代码，不做额外动作

收到「修改 / 创建 / 实现」类需求时，**只完成代码本身**：

- ❌ 写代码过程中不要跑验证（tsc / 构建 / 单测）。
- ❌ 不要写测试、检查脚本、说明文档。
- ❌ 不要提交 git（不 `commit` / `push`）。
- ❌ 不主动调用预览、不主动打开浏览器（用户自行预览）。
- ✅ 验证 / 测试 / 文档 / 提交，仅在**用户明确要求**时才做。

> 例外：若代码改动涉及「跨文件类型签名」「之前报错过的坑」，可保留一次轻量 tsc 仅看自己改动的文件；但这不属于默认动作，默认就是「写完即止」。

---

## 2. 样例数据 = 本地 JSON（全项目强制执行）

- 所有页面的样例 / 驱动数据，**写成 JSON 格式保存到本地**（统一落在 `src/console/*.json` 或等价位置）。
- 通过本地 JSON 持久化端点读写（如 `/api/save-*`、`/api/load-*`），首启动用 SEED 自动落盘。
- **已落地模块**：贷前审核、报告模板。
- **新模块一律沿用此约定**，不复用其他数据载体（不嵌死在代码里、不依赖远程接口样例）。

---

## 3. 数据来源标签（全项目强制执行）

新建页面**一律显示数据来源标签**，三色规则固定：

| 颜色 | 标签 | 含义 | 作者 |
|---|---|---|---|
| 🟠 橙 | `Sam`（样例） | 本地样例 JSON | 使用域作者 |
| 🔵 蓝 | `Cfg`（配置） | 配置文件 / 管理中心配置 JSON | 配置作者 |
| ⚪ 灰 | `Cal`（实时计算） | 聚合 / 公式 / 派生结果 | 运行时计算 |

- 复用 `src/console/SourceTag.tsx` 提供的 `<Cfg> / <Sam> / <Cal>` 组件，不要重新发明标签 UI。
- **已落地模块**：贷前审核、报告模板。
- **已落地**：全局开关 `showSourceTags`（见 `src/console/sourceTagConfig.ts`，默认 `true`）；`SourceTag` 的 `<Cfg>/<Sam>/<Cal>` 在该开关关闭时自动隐藏，并新增 `<SourceTagLegend>` 页面级图例（蓝=配置 ｜ 橘=样例 ｜ 灰=实时计算）。生产环境可 `setShowSourceTags(false)` 关闭标签。

---

## 4. 复用优先

- 规则 2、3 已有成熟实现，**新需求优先复用既有组件与模式**，不要重复造轮子：
  - 标签：`SourceTag.tsx` 的 `<Cfg>/<Sam>/<Cal>` + `<SourceTagLegend>` 图例
  - 页面骨架：`PageShell.tsx` —— 新建页面把 `<PageHeader>` 换成 `<PageShell>` 即自动带标签图例，无需每页手贴
  - 持久化：`midStore.ts` 的「配置/样例分文件」模式 + `/api/load-*` `/api/save-*` 端点
  - 配置驱动渲染：管理中心配置 → 使用域按配置渲染（神策 BI 模式）
- 具体「哪些既有实现可以复用、怎么复用」会在后续需求提出时具体确认。

---

## 5. 专家补充建议（非强制，供决策参考）

下面几条是作为「项目架构顾问」的补充意见，不强制，但建议在规划新模块时纳入考量：

1. **「完全不验证」的代价**：纯写代码不验证，类型错误 / 导入缺失 / 字段名拼错会沉默积累，最终一次性爆发。建议保留一个「收尾一次性 tsc（仅看自己改动文件）」作为安全阀，平时不跑、不动声色。
2. **把标签做成「页面骨架」而非「逐页手贴」**：新建页面时直接套一个 `PageShell` / 样板，标签、JSON 落盘、布局一次性就位，避免每个页面重复粘贴 `<Cfg>/<Sam>/<Cal>` 和持久化接线。
3. **全局开关 `showSourceTags` 顺手就做**：它改造成本极低（一个 store 布尔 + 条件渲染），但能立刻让「开发态全标签 / 生产态干净」可切换，建议尽早落地，别等。
4. **样例 JSON 也要有「契约」**：建议每个 `*.json` 配一份轻量类型/`zod` 校验（仅 dev 期），防止样例结构漂移导致页面崩。可复用 `midData.ts` 的 `SEED_*` + 类型定义模式。
5. **配置与样例的「作者边界」要写进注释**：蓝（Cfg）是配置作者维护、橘（Sam）是使用域作者维护，两者可能被不同人改；在 JSON 文件头加一行注释说明归属，减少误改。

---

_本规范由用户于 2026-08-04 提出，覆盖全部会话。任何与之冲突的默认行为，以本规范为准。_
