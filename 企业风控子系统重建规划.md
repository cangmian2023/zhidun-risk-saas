# 企业风控子系统（v3）重建规划与评估

> 重建目标：把 `record/qixin` 下「风控 / 尽调 / 企业档案」快照 **1:1 复刻**进系统，合并进「企业风控」子系统，旧企业风控页面全部下线。

## 一、源材料盘点
`record/qixin/` 下是某企查类 SaaS 导出的 **HTML DOM 快照**（非可运行代码）。按前缀统计：

| 来源 | 页面文件数 | 说明 |
|---|---|---|
| 风控 - * | 27 个 | 风险预警 / 监控列表 / 统计看板 / 风险地图 / 监控管理 / 定期体检 / 账户年检 / 监管合规 / 财产线索 / 员工列表 / 利益排查 / 黑名单 等（含详情/结果子页） |
| 尽调 - * | 22 个 | 企业尽调 / 批量尽调 / 关系尽调 / 人员尽调 / 报告中心（含人员详情、关联方识别结果等子页） |
| 企业档案 - * | 18 个 | 基本信息 / 历史 / 司法 / 舆情 / 经营 / 经营风险 / 集团 / 企业图谱(8 子图) / 财产信息 |
| 菜单定义 | 2 个 | `风控子系统目录`、`尽调目录`（原始产品菜单） |

「1:1 复刻」= 用系统现有 React 组件把每个快照的 **页面结构、字段、表头、Tab、按钮、分区** 重做出来，数据走本地样例 JSON（橘 Sam）。

## 二、关键决策（已与用户确认）
1. **范围扩大**：一并纳入「企业档案」18 页 → 总业务页约 **49 个父页面**（子快照折叠进父页）。
2. **1:1 还原至高无上**：含地图 / 关系图 / 复杂图表页，均按原样还原（不简化）。
3. **旧页处理**：旧 6 个源文件已备份到 `src/console/_legacy_ep/` 后删除；`enterpriseData.ts/json`（被管理中心 / 评分 / 贷中预警共享）**保留**。

## 三、目标结构（合并进企业风控，沿用 `ep:` 路由）
| 中心 | 路由前缀 | 页面（菜单登记 key） |
|---|---|---|
| 风控中心 | `ep:fk-*` | 风险预警 / 监控列表 / 统计看板 / 风险地图 / 监控管理 / 定期体检 / 账户年检 / 监管合规 / 利益排查 / 黑名单 / 员工列表 / 财产线索 |
| 尽调中心 | `ep:jd-*` | 企业尽调 / 批量尽调 / 关系尽调 / 人员尽调 / 报告中心（+ 结果/详情/模板子页） |
| 企业档案 | `ep:arc-*` | 基本信息 / 历史 / 司法 / 舆情 / 经营 / 经营风险 / 集团 / 企业图谱 / 财产信息 |

## 四、技术底座（已落地）
- **分发**：`EnterpriseModule` 按前缀 `fk-/jd-/arc-` 分发到三大模块；各模块用 `import.meta.glob('./pages/*.tsx')` 懒加载，新增页面只需放文件（文件名 = 路由去掉前缀，如 `fk-employee` → `pages/employee.tsx`），无需改共享代码。
- **错误隔离**：`EpErrorBoundary` 单页崩溃不影响整个子系统。
- **共享脚手架** `enterprise/epCommon.tsx`：`EpPage`（标题+三色图例）、`EpCard`、`EpStat`、`EpTag`、`EpBtn`、`EpDrawer`、`DataTable`/`Panel` 再导出、`useSample(file, seed)`（本地 JSON 走 `/api/load-mid`）。
- **样例数据**：JSON 放 `src/console/` 根（vite 中间件只解析该目录 `.json`），如 `fkEmployee.json`。

## 五、分批实现规划
| 批次 | 内容 | 状态 |
|---|---|---|
| 第 0 批 | 底座：删旧页、建目录、重写分发/菜单/入口、脚手架 | ✅ 完成 |
| 第 1 批 | 风控基础：员工列表+添加、利益排查（+详情）、定期体检+设置 | 🔧 员工列表/利益排查已完成，其余进行中 |
| 第 2 批 | 风控：监控列表、风险预警全套、财产线索全套 | 待实现 |
| 第 3 批 | 风控可视化攻坚：统计看板、风险地图、监控管理-详情 | 待实现 |
| 第 4 批 | 尽调基础：人员尽调、企业尽调+结果、批量尽调、报告中心 | 待实现 |
| 第 5 批 | 尽调详情/图谱：人员详情全套、批量画像、关系尽调全套 | 待实现 |
| 第 6 批 | 企业档案模块（含图谱） | 待实现 |
| 第 7 批 | 联调收尾：三色标签全量、导航回退、整体走查 | 待实现 |

## 六、工作量评估
- 简单页（单列表/单表单）：约 22 个
- 中等页（多 Tab / 多表 / 详情卡）：约 18 个
- 复杂可视化/图谱页（地图、关系图、SVG）：约 9 个（含 3MB 巨型页，重点攻坚）
- 折算「页面当量」≈ 120+，分 8 批，每批可独立验证。
