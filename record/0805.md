你只改变每条bug的状态，不在文档内容做修改

- [ ] 1. 修改管理中心的左侧菜单栏

除了规则集合，其他页面暂时不需要分级

- [x] 2. 补充详情页

> 好的，我会再详细看看你的文档的。你先按照文档生成页面和功能吧。先完成管理中心的功能，注意：
> 
> 1. 所有页面先完成功能，然后写样例数据的json，json保存到本地。
> 2. 页面都标标签：数据来源是样例json 的 是橘色，是配置json（就是管理中心的样例json）的是蓝色，实时计算的是灰色
> 3. 监控策略配置 的 监控内容来自指标，配置的业务流程、规则等要用json保存到本地（橘色）
> 4. 监控页面配置  的 监控内容来自指标，配置的页面和可视化组件等要用json保存到本地（橘色）
> 5. 监控看板 的 的页面和可视化组件等 读取 监控页面配置的json（蓝色），创建样例json保存本地（橘色）
> 6. 预警工作台 的业务流程、规则等 读取 监控策略配置的json（蓝色），创建样例json保存本地（橘色）
> 7. 个体详情页 先完成功能，创建样例json保存本地（橘色）
> 8. 处置工单页  先完成功能，创建样例json保存本地（橘色） 懂的话就可以开始做了，祝你好运
> 
上面是之前的需求，根据这个需求和业务文档，完成了和贷中监控有关的页面。但是这些页面只是完成了了列表页，没有列表内容的详情页。

先从管理中心的相关页面开始，完成数据源、指标、策略配置、看板配置的详情内容查看。完成内容规划、交互设计，最终形成前端页面。查看详情页，也遵循规则，内容数据保存json本地。

- [ ] 3. 数据来源标签：由「页面级」下沉为「字段级」
  - 现状：管理中心 / 贷中相关页面目前仅在整页级别标注一个数据来源标签（页面级）。
  - 需求：改为字段级——每个数据字段 / 指标 / 配置项单独标注其数据来源（样例 JSON / 配置 JSON / 实时计算）。
  - 来源分类与配色沿用：样例数据 JSON = 橘、配置 JSON（管理中心配置）= 蓝、实时计算 = 灰；全局开关 `showSourceTags` 仍控制整体显隐。
  - 实施：页面标注从页面级改为字段级（`SourceTag` 下沉为可包裹单个字段的组件，来源信息随字段数据从 JSON 带入）；并把上述要求落到本文档。
  - 待澄清：具体标注哪些页面的哪些字段、标签形态（行内角标 / 悬停提示）。

- [ ] 4. 以下看一下数据源管理
- [ ] 4.0 我看到你做了一些数据来源标签，但是标签的内容完全不对吧？配置JSON·配置JSON 能表示来自哪个字段吗？正确的不应该是：JSON:NAME=TIANTIANTIAN;配置:配置：sample1.name1=xxxx
- [ ]4.1 列表页：你确定列表页真的读取 本地 json文件了吗？为什么样例JSON·样例数据:23 行 但是只显示 了4行数据呢？？
- [ ] 4.2 列表上的字段：名称	类型	状态	字段数		更新时间	操作。这些字段配置的都来自 配置JSON 。。数据源是最基础的配置了，为啥要来自配置JSON 啊。。而且这个表格里的，不都应该是 本地 json 吗？

- [ ] 4.3 本地详情的页面内容，有哪个样子了，但还是不对。缺少基本信息。就是如果是数据源的话，数据源 ip ，username，password 这一系列 配不配？在哪里现实呢？？ 所以这个页面重新设计以下。先详情页的设计文档，再根据设计文档完成功能，注意生成功能事的前端页面不要写值，只在只的位置写标签。然后根据功能生成样例数据。功能页面，关联样例json。这样我加载的页面，就会是正确的了

- [ ] 4.4 如果你理解了4.3，那根据4.3 重新审核以下4.2 是不是正确的。

- [ ] 4.5 详情页：按钮为什么又来源配置呢 配置JSON·配置JSON:midDataSources.json？ 字段清单为什么是实时计算的呢（实时计算·字段口径）？字段清单 这个列表里的字段，命名时需要配置或者读取后保存到本地的json为什么来自 配置JSON·配置JSON 。 

- [ ] 5. 指标库 的修改
- [ ] 5.1 指标列表  按照 数据源列表 修改
- [ ] 5.2 指标详情，显示的内容和功能不对，重新做指标详情的功能设计文档。功能规划文档，要考虑清楚指标的各种状态和对应的操作。并根据功能，重新做页面，并且页面功能和样例数据要分开。具体参考 4.3 要求。样例数据，要各种状态的数据都包含，列表和详情都要显示出区别，并关联对应。
- [ ] 5.3 指标要根据引入的数据源生成，但是现有生成完全没有体现出来。要把这个需求考虑清楚。在实现这个功能的时候，这个地方要从数据源的列表的json中去选择，详情也要显示数据源的json详情。
- [ ] 5.4 要针对贷中监控的场景，准备充分、完整的指标的样例数据

- [ ] 6. 管理中心，所有左侧菜单的1级页面，时列表页的，第一个字段和最后一个字段 操作列，固定。中间可拖动。字段不要缓缓。操作列，自适应宽度，向左对齐。
- [ ] 7. 监控策略配置 的修改
- [ ] 7.1 监控策略配置 改名为 策略配置
- [ ] 7.2 列表页、详情页 汇总整理 数据源和指标库的修改路径进行修改。
- [ ] 7.3 监控策略配置 监测的内容应该 是指标。但是现有功能完全没有体现，在功能规划中要考虑清楚。然后再实际功能中，引入或关联指标时，要选择指标列表的json。监测详情，要显示引入指标或关联的json
- [ ] 7.4 功能规划，要考虑清楚状态和对应的操作，然后编写丰富的样例数据，覆盖全部状态。
- [ ] 7.5 要针对贷中监控的场景，准备充分、完整的策略配置。配置要关联正确的，已有的指标样例数据

- [ ] 8. 监控页面配置 的修改

- [ ] 8.1 监控页面配置 名称 修改为 页面配置
- [ ] 8.2 总结7部分，完成页面配置的修改。
- [ ] 8.3 要针对贷中监控的场景，准备充分、完整的监控页面配置 。配置要关联正确的，已有的指标样例数据

- [ ] 9. 尽可能页面复用，一致；

- [ ] 10. 在页面操作添加、编辑删除，要更新对应的json数据

- [ ] 11. 回顾第4.x 问题：
- [ ] 11.1 	本地样例 是模拟数据，现实需求并没有这个类型。
- [ ] 11.2 已经明确而说过了。你要标注出json 的来源。
- [ ] 11.3 样例JSON:所有数据源样例行总和 27 行 . 这里显示27行，但是实际列表中只有6行，说明你没有读取json数据，列表中显示的数据还是你写死的。
- [ ] 11.4 列表中的数据来源标签，全部错误。也表示你并不是读取的json。是的，需求是，列表要读取样例数据的json。但是显示的都是配置的json。那你这个配置的json是哪里来的呢？？？数据源是最底层的模块，没有功能为数据源做配置。
- [ ] 11.5 字段清单 只有 字段key 列 标了数据来源，还是错的。这个地方所有字段，都要标来源标签，而且都是来自样例json而不是配置json。
- [ ] 11.6 整个页面，所有来源都来自样例json，而不是配置json
- [ ] 11.7 所有标注来自配置json的，你自查以下，这部分数据针对是读取了本地的json文件，还是写死到页面中了。如果写死到页面中了，是不满足需求的。需求是：功能和数据分开。需要访问数据的地方，标注来源字段标签，然后读取本地的json文件，显示标签上的对应字段。
- [ ] 11.8 读懂以上，总结、修改。然后审查5-10是否满足需求，审查出的问题，及时修改
- [ ] 11.9 这几个页面用同一个模板，方便维护


- [ ] 12. 整理定制业务流程的功能
之前在报告模板 - 人工审核 - 列表 - 业务流程配置列，实现了业务流程的配置。（页面地址：http://localhost:5173/console/cm/report-template）
操作流程是：点击添加流程，在弹窗窗口，显示流程画布，配置节点，完成节点属性配置，保存。之后可以编辑删除。配置好的业务流程可以保存到本地。
配置好的业务流程，会在信息核验（http://localhost:5173/console/cr/pre-verify-222）、信用风控、欺诈识别、进件审核中的列表-操作列的按钮，详情页的第二个卡片，以按钮的形式显示；
点击按钮，弹窗内容和配置内容保持一致。点击审批按钮，会根据不同状态，进入下一个节点，下一个节点显示状态、按钮、弹窗页符合配置要求。以此类推。直到最后一个节点。

注意我提示的页面地址，去检查代码，我已经大概复述了功能，你按照代码，总结完整的功能技术流程。并生成一个新的功能页面：业务流程配置，放到管理中心最下面。然后实现全部需求。

在实现需求的过程中，功能和样例数据分开实现，和其他页面保持一致。