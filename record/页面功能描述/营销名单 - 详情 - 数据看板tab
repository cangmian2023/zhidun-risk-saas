# 营销名单-主题详情页｜数据看板 Tab 完整字段级PRD（前端交付版）
> 归属路由：营销名单 → 乡村振兴 主题详情 →【数据看板】Tab；配套同级Tab：全部线索；
> 产品约束：和【全部线索】Tab、营销名单列表页共用一套组件库、配色、交互规范；筛选切换、导出、排序逻辑保持统一。

## 一、页面完整分层架构（自上而下层级划分）
```
【面包屑导航区】
【Tab切换头部区】 ├─ Tab标签：全部线索 / 数据看板（当前激活）
                  └─ 右上角全局操作按钮：添加企业
【看板筛选栏】      线索筛选下拉组件
【全局指标汇总卡片区】 四大横向统计指标卡片（和全部线索Tab指标一致，数据联动）
【模块1：部门营销数据板块】
  ├─ 板块标题、图表分析切换按钮、统计口径说明、导出按钮
  └─ 部门营销数据表格（支持表头排序）
【模块2：个人排行榜板块】
  ├─ 板块标题、图表分析切换按钮、统计口径说明、部门筛选下拉、导出按钮
  └─ 个人排行榜表格（支持表头排序）
【兜底通用组件】    加载骨架屏、空数据状态、接口报错重试、Tooltip悬浮提示
【分页补充说明】   本页面两大统计表若无大量数据，可不用分页；超量需增加分页控件（截图未展示）
```

## 二、逐区域详细拆解（布局、组件、字段、交互、样式、边界规则）
### 区域1：面包屑导航（页面最顶部）
- 文本内容：`营销名单 / 乡村振兴 主题详情`
- 交互：“营销名单” 蓝色可点击，返回【营销名单列表主页】；中间名称纯展示不可点击
- 样式：次要灰色导航文字，`/` 作为分隔符

### 区域2：Tab切换 + 右上角主操作按钮
1. Tab标签组（横向）
    - Tab1：全部线索（未激活）
    - Tab2：数据看板（当前激活，下划线高亮）
    - 交互：点击Tab切换页面内容；**切换Tab时保留筛选条件**，切换后重置内部表格至第一页
2. 右上角主按钮：【+ 添加企业】
    - 组件：黄色主按钮，全局统一色 `#ffc53d`，前置加号图标
    - 交互：点击唤起弹窗/跳转企业选择页，向当前营销名单新增企业线索；操作完成后看板统计数据自动刷新

### 区域3：看板筛选栏（Tab下方，指标卡片上方）
- 筛选组件：线索筛选分组下拉：**更新日期、创建日期**
- 交互规则
  1. 下拉选择时间区间，确认后整体刷新下方【指标卡片、部门表格、个人排行榜表格】全部数据
  2. 筛选变更后，表格重置到第1页
  3. 筛选条件支持回显，Tab之间可携带筛选参数

### 区域4：全局指标汇总卡片区（横向4组指标卡片，和【全部线索】指标完全对齐，数据同源联动）
> 布局：横向自适应，窄屏支持横向滚动；指标数字支持hover提示，可扩展点击联动筛选
1. 总线索
    - 主指标：总线索 20条
    - 细分指标：未分配 19 | 已分配 1
2. 待营销
    - 主指标：待营销 0条
    - 细分指标：30天未跟进 0 | 近7日新增 0
3. 营销中
    - 主指标：营销中 1条
    - 细分指标：有限进记录 0 | 无跟进记录 1
4. 营销结束
    - 主指标：营销结束 0条
    - 细分指标：成功 0 | 失败 0 | 无需营销 0
- 交互：筛选条件变更，卡片数字实时刷新；数字加粗高亮，辅助文案浅灰色

### 区域5：模块一｜部门营销数据
板块内部结构：标题栏 + 数据表格
#### 5.1 板块标题栏（一行横向布局）
1. 左侧：标题 `部门营销数据` + 【图表分析】切换按钮
    - 交互：点击【图表分析】，表格切换为可视化图表视图（柱状图/折线图），再次点击切回表格视图
2. 中间：统计口径说明文本：`统计口径: 全部名单及线索, 其中包含了个人可见的名单及线索。`（浅灰色辅助文字）
3. 右侧：【导出】按钮
    - 功能：导出当前筛选下完整部门营销表格数据，导出Excel；大数据量支持异步导出任务

#### 5.2 部门营销数据表格
##### 表头顺序（支持表头排序）
序号 → 部门 → 待营销 → 营销中 → 有限进 → 营销成功 → 跟进次数 → 活跃次数
- 可排序字段：待营销、营销中、有限进、营销成功、跟进次数、活跃次数；表头展示升降箭头，点击切换asc/desc
- 序号：固定排序，不可排序

##### 单行单元格渲染规则
1. 序号：排名序号（1、2、3、4），搭配排名标识色块（红、绿、蓝、浅灰）
2. 部门：蓝色可点击链接，可跳转该部门下线索明细（可选权限控制）
3. 待营销 / 营销中 / 有限进 / 营销成功 / 跟进次数 / 活跃次数：纯数字；无数据填充 `-`

### 区域6：模块二｜个人排行榜
板块内部结构：标题栏 + 数据表格
#### 6.1 板块标题栏（一行横向布局）
1. 左侧：标题 `个人排行榜` + 【图表分析】切换按钮
    - 交互：点击切换 表格视图 ↔ 可视化图表视图
2. 中间：统计口径说明文本：`统计口径: 全部名单及线索, 其中包含了个人可见的名单及线索。`
3. 右侧组件组：【选择部门】下拉筛选器 + 【导出】按钮
    - 选择部门：下拉单选部门，筛选仅展示该部门人员排行；默认全部部门
    - 导出：导出当前筛选下个人排行榜全量数据Excel，支持异步导出

#### 6.2 个人排行榜表格
##### 表头顺序（支持表头排序）
序号 → 姓名 → 部门 → 待营销 → 营销中 → 有限进 → 营销成功 → 跟进次数 → 活跃次数
- 可排序字段：待营销、营销中、有限进、营销成功、跟进次数、活跃次数
- 序号：排名序号，带排名标识色块

##### 单行单元格渲染规则
1. 序号：排名序号，搭配排名标识色块
2. 姓名：脱敏展示（示例：19156027703），hover展示完整信息
3. 部门：文本展示归属部门
4. 待营销 / 营销中 / 有限进 / 营销成功 / 跟进次数 / 活跃次数：数字，支持附带百分比（示例：1 5.00%）；无数据填充 `-`

### 区域7：全局兜底状态（截图未展示，开发必须实现，否则返工）
1. 加载状态：页面整体骨架屏，匹配两大表格布局
2. 空数据状态：筛选后无数据，展示空图文 + 重置筛选按钮
3. 接口异常：错误提示文案 + 重试按钮
4. 超长文本：单元格内容超出宽度自动省略，hover悬浮Tooltip展示完整内容

## 三、全局设计&样式规范（整套系统统一）
1. 主色：黄色 `#ffc53d`（添加企业主按钮，和全平台保持一致）
2. 文字层级
    - 主指标数字、表格重点内容：#333 加粗
    - 表格正文、板块标题：#333
    - 统计口径说明、辅助文字：#666
    - 占位提示文字：#999
3. 排名标识配色：第1红色、第2绿色、第3蓝色、4及以后浅灰色
4. 表格样式：浅灰分割线；行hover浅底色高亮
5. 弹窗、抽屉、导出任务弹窗：复用整套后台统一组件

## 四、接口入参参考（后端对接）
```json
{
  "listId": "营销名单唯一ID（固定）",
  "updateDate": "更新日期筛选区间",
  "createDate": "创建日期筛选区间",
  "deptFilter": "个人排行榜部门筛选",
  // 部门表格排序参数
  "deptSortField": "营销中",
  "deptSortType": "asc/desc",
  // 个人排行表格排序参数
  "userSortField": "活跃次数",
  "userSortType": "asc/desc"
}
```

## 五、截图缺失，开发必须补充的功能清单（缺失造成返工）
1. 【图表分析】视图：部门、个人排行的可视化图表渲染（柱状图等）
2. 大量数据场景下，两张统计表增加分页控件（和系统统一分页组件）
3. 导出完整流程：异步导出、任务中心下载、导出字段配置弹窗
4. 权限控制：不同角色隐藏导出、部门点击跳转等功能
5. 排名指标点击联动筛选，跳转全部线索Tab自动带入筛选条件
6. 图表视图和表格视图切换时，保留排序、筛选条件

## 六、跨页面联动说明
1. 面包屑【营销名单】→ 返回营销名单列表主页
2. Tab【全部线索】→ 切换至线索明细列表页面
3. 部门单元格点击 → 携带部门筛选参数跳转【全部线索】Tab自动过滤对应部门线索
4. 筛选条件变更后，全部统计卡片、两张表格同步刷新数据

