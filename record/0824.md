# 页面结构问题


1. 页面顶部（在页面标题上面）有个莫名其妙的白框：
/grid-marketing /grid-marketing /tender /supply-chain /techfin
2. 页面标题没有吸顶：
/company-lib 
2. tab没有吸顶：
/group-account /person-archive-basic /ent-archive-basic
3. 列表标题行两个颜色
/market-list  /market-list /market-list-detail?back=

# 风险地图 /fk-map
1. 地图展位要和 右边4个统计概览一样高

# 定期体检 /fk-health-check
1. 初始化页面，2个区块离太近，加一下进军 

# 账户年检 /fk-account-yearly
1. 初始化页面，2个区块离太近，加一下进军 
 
# 财产线索 /fk-property
1. 模拟增加一些扩大主题、资产状况的数据
2.  tab条样式不好看，优化一下
3. tab线索信息 75 下面内容各个区块间距过近，优化一下
3. 表格问题：
Bug 现象

表格【操作】列配置了**右侧固定（fixed）**，横向滚动表格时，部分行内「线索内容」文本溢出，渲染到了固定操作列的右侧区域，内容布局错乱，遮挡页面空白区域，视觉异常。

> 补充说明：固定列本应悬浮在表格最右侧，表格主体内容滚动时，不能穿透渲染到固定操作列的右边。

预期效果

1. 横向滚动表格时，【操作】固定列始终停靠在表格最右侧；
2. 表格主体单元格内超长文本，只能在主体滚动区域内展示，**不允许渲染到固定操作列的右侧**；
3. 单元格文本按照规则截断 / 换行，不会穿透固定列边界。

# 企业尽调报告 /jd-company
1. 现在修改错了，之前有一个首页，有一个大的输入框，然后输入企业名称，进入企业尽调报告。现在打开直接显示企业尽调报告，这错了
2. tab 工具条 不用垂直滚动条，要自适应高度；水平滚动条不显示，要最左、最右各一个 箭头按钮，来顺位移动tab按钮
3. 所有tab内容都显示在页面中，tab 按钮起到水平滑动页面的效果，不要切换隐藏

# 批量尽调结果 /ep/jd-batch-result?back=
1. 查企业 搜索结果没有数据，编造一些模拟数据
2. 查人员 的搜索工具条没有了，回复一下

# 利益排查 /fk-interest
1. 查询区的效果优化

### . 整体布局调整

1. 主结构改为 **顶部流程步骤导航 + 下方左右双上传卡片**，强化业务先后顺序
流程步骤：①上传员工名单 → ②上传合作方名单 → ③发起利益冲突排查
> 
> 中间原有「董监高法 → 姓名重名 → 电话关联企业 / 持股任职控制」改为**底部小字辅助说明：本次排查规则**，不要占用主视觉区，避免干扰上传操作
2. 两个上传卡片保持左右等分布局，统一圆角、内边距、标题样式，视觉对齐
3. 「收起配置」按钮固定在**模块右上角**，保持原有功能不变

### 2. 卡片内部信息优化（左右卡片统一规范）

卡片结构从上到下：

- 图标 + 模块标题（员工名单 / 合作方名单）
- 辅助描述（上传企业内部员工名单）
- 容量限制提示：单次最多支持 ≤2000 人 / 家（弱化展示，灰色小字）
- 操作按钮：【添加员工名单 / 添加企业名单】（主按钮样式，突出上传动作）
- 新增状态反馈区：上传后展示「已上传文件名称、数据行数、删除 / 重新上传」

### 3. 核心操作按钮「排查利益冲突」优化

1. 位置调整：不再悬浮在卡片中间，移动至**整个配置模块底部居中**
2. 交互逻辑：
   - 员工、合作方名单**均完成上传前**：按钮置灰，hover 显示 tooltip 提示：请先上传员工名单与合作方名单
   - 两份文件上传完成后：按钮激活为主色按钮，可点击发起排查
3. 上传进度 / 计数展示：「已上传：员工名单 X 人 / 合作方名单 X 家」放在排查按钮上方，清晰展示上传数量

### 4. 原有中间标签处理

原虚线标签 `董监高法、姓名重名、电话关联企业、持股/任职/控制`
👉 优化：下沉为【排查规则说明】折叠面板 / 浅灰色小字备注，放在排查按钮下方
作用：告诉用户上传后系统会做哪些维度比对，不打断上传主流程

### 5. 视觉细节统一

- 统一卡片圆角、阴影、内边距，消除左右卡片视觉轻重不一致
- 「查看样例」统一放在卡片右上角，作为次要文字链接
- 信息分层：标题 > 操作按钮 > 说明文字 > 容量小字，使用不同字重、色值区分层级

# 报告中心 /jd-report
1. 这个页面不对，之前是一个有自定义报告功能的 报告列表卡片页面。现在打开是一个 企业尽调报告页面，恢复到旧版


=========

# 企业库 / 产业引导基金  company-lib-list?back=
1. 最新商机列 太子 ，每行颜色不一样



# 招投标 /tender
1. 增加一些样例数据

# 供应链 /supply-chain
1. 查询工具条 和白底顶部贴在一起，视觉效果不好，修复一下
2. 操作列太窄
 
# 科创金融 /techfin
1. 查询工具条 和白底顶部贴在一起，视觉效果不好，修复一下
2. 详情页 的页面标题没有吸顶(这个问题很多页面都有，能处理一下吗)
Bug 现象

页面顶部**页面标题区域、Tab 标签栏（企业概况 / 科创能力分析等）** 配置了吸顶固定效果；
滚动页面时，下方【企业概览、多维能力图】等内容元素**层级高于吸顶区域**，滚动后向上穿透、覆盖在吸顶标题 / Tab 栏上方，遮挡吸顶导航，交互视觉异常。

预期效果

页面标题、Tab 标签栏滚动吸顶后，**z-index 层级高于页面下方所有内容**，下方滚动内容被吸顶栏遮挡，不会向上穿透覆盖导航。

3. 详情页的2级tab工具条要自动换行，不显示水平滚动着
4. 详情页 很多2级tab 没有内容补充完整合适的内容
5. 详情页 1级tab 内容区 显示全部内容，2级tab启动滑动页面效果，不是切换隐藏效果

# 营销名单 /market-list
1. 操作列换2航线是

# 文旅消费复苏 主题详情 //market-list-detail?back=
1. 操作列标题 靠左
2. 删除 AI+ 按钮
3. 更多…按钮的弹窗被遮盖
4. 这个页面的按钮和其他页面颜色不一致


# 存客商机 /exist-biz
1. 列表第一列 和第二列 颜色不一致
2. 下面是筛选控件，显示错误。把内容改为筛选控件，并填补对应内容

部门人员
∨

客商标签
∨

客商分组
∨

客商数据范围
∨

有无备注
∨

添加时间
∨

合同开始时间
∨

合同到期时间
∨

付款日期
∨


# pevc /pevc
1. 最底部有一个水平滚动轴，删除这个滚动轴，三个tab的列表自适应宽度
2. 投融资事件和投资基金列表的tab里 列表 颜色不一致

# 同业分析 /peer-analysis
1. 优化一下筛选

# 金融法规 /fin-law
1. 补充一下 处罚案例


# 有哪些企业评分

# 企业尽调 - 尽调报告 /jd-company

1.1 综合得分 点击弹窗，弹窗内容 复刻：record\功能分解\综合得分.HTML

2. 企业指数 从原来的五个 修改为 经营指数  、空壳指数  、科创分、合同违约指数、司法风险

2.1 经营指数   点击弹窗，弹窗内容 复刻：record\功能分解\企业指数 .html

2.2 空壳指数  点击弹窗，弹窗内容 复刻：record\功能分解\空壳指数.html
空壳指数是从企业经营场所、资产形态、企业人员、经营活动、经营资质、风险信息等维度，扫描空壳特征，用于供应链管理、信贷风控、经济犯罪侦查或者税务督查等应用场景。指数范围0-100，分值越大，主体是空壳的概率越高。


2.3 科创分   点击弹窗，弹窗内容 复刻：record\功能分解\科创分.html
科创企业评分，是从企业技术创新、科创资质、研发实力、企业成长性以及行业潜力5个大类（20+个细分维度）维度综合评价企业的科技创新能力以及发展潜力。辅助金融机构、政府/产业园等多种业务应用场景决策。

2.4 合同违约指数  点击弹窗，弹窗内容 复刻：record\功能分解\合同违约指数.html
温馨提示：指数对于合同违约规模的量化会考量主体规模，即便违约规模很小，小公司仍然有可能被评估为高风险


2.5 司法风险   点击弹窗，弹窗内容 复刻：record\功能分解\司法风险.html 
